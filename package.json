{
  "name": "dewbox",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-09-08",
    "deploy": "wrangler pages deploy public --project-name=dewbox"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
